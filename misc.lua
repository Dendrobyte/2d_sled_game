-- Random shit

-- Code sample for loading a sprite sheet image and defining coords if I want to go that route
-- spritesheet = love.graphics.newImage("spritesheet.png")

-- -- Define a quad (portion of the spritesheet)
-- -- Assume each sprite is 32x32 pixels and you want to use the sprite at (x, y) = (32, 0)
-- spriteWidth, spriteHeight = 32, 32
-- quad = love.graphics.newQuad(32, 0, spriteWidth, spriteHeight, spritesheet:getDimensions())
